{
  "name": "termlist-db",
  "version": "0.2.0",
  "description": "Some kind of dictionary",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker-up": "cp -R packages/termlist-web packages/termlist-server/termlist-web && docker-compose up && rm -rf packages/termlist-server/termlist-web",
    "docker-up-build": "cp -R packages/termlist-web packages/termlist-server/termlist-web && docker-compose up --build && rm -rf packages/termlist-server/termlist-web"
  },
  "repository": {
    "type": "git",
    "url": "git+https://16patsle@github.com/16patsle/TermListDB.git"
  },
  "author": "Patrick Sletvold",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/16patsle/TermListDB/issues"
  },
  "homepage": "https://github.com/16patsle/TermListDB#readme",
  "devDependencies": {
    "lerna": "^2.11.0"
  },
  "dependencies": {
    "termlist-server": "file:packages/termlist-server"
  }
}
